import { arbolConIterator } from "../version-arbol-con-iterator";
import { arbolSinIterator } from "../version-arbol-sin-iterator";
import { arrayConIterator } from "../version-array-con-iterator";
import { arraySinIterator } from "../version-array-sin-iterator";

export const patronIterator = () => {
    // arraySinIterator();
    arrayConIterator();
    // arbolSinIterator();
    // arbolConIterator();
}